Run these commands in command prompt

pip install tensorflow
pip install tensorflow-hub
pip install numpy
pip install matplotlib
pip install beautifulsoup4
pip install requests
pip install urllib3
pip install fake-useragent
pip install django
pip install django-crispy-forms
pip install google-cloud-vision

Use this link to download the model
https://storage.googleapis.com/tfhub-modules/google/universal-sentence-encoder/1.tar.gz

Unzip it and paste it in fake_news/fake/model

Paste images in fake_news/fake/static to use them